import {$settings} from "./api/$settings";
import {$kv} from "./api/$kv";
import {$overlay} from "./api/$overlay";

/** In order to consume an event, an event handler function can be registered in the broadcast overlay by using $overlay.on */